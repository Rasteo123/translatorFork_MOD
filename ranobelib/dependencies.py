import sys

REQUIRED_LIBS = {
    "python-docx": "docx",
    "PyQt6": "PyQt6",
    "playwright": "playwright",
    "beautifulsoup4": "bs4",
    "lxml": "lxml",
}


def check_dependencies():
    missing = []
    for pip_name, import_name in REQUIRED_LIBS.items():
        try:
            __import__(import_name)
        except ImportError:
            missing.append(pip_name)
    if missing:
        print("\n╔══════════════════════════════════════════════╗")
        print("║   ОШ�?БКА: Не все библиотеки установлены      ║")
        print("╠══════════════════════════════════════════════╣")
        print(f"║ Отсутствуют: {', '.join(missing):<32}║")
        print("╠══════════════════════════════════════════════╣")
        print("║ pip install " + " ".join(missing) + " " * max(0, 33 - len(" ".join(missing))) + "║")
        print("║ playwright install chromium                   ║")
        print("╚══════════════════════════════════════════════╝\n")
        sys.exit(1)

