# gemini_translator/utils/__init__.py
# -*- coding: utf-8 -*-

"""Пакет утилит.

Здесь же поднимается канал «код → пользователь»: обработчик встаёт на корневой
логгер один раз, при первом импорте чего угодно из `utils`, и дальше любому
модулю достаточно обычного `logging.getLogger(__name__).warning(...)`, чтобы
сообщение дошло до окна лога. Саму шину канал находит сам — см. user_log.
"""

from . import user_log as _user_log

_user_log.install()
